# 👇 Back-End Variables

# URL completa de conexión a PostgreSQL (ajustar según entorno o Render)
DATABASE_URL=postgresql://usuario:contraseña@host:puerto/nombre_db

# Clave secreta para autenticación JWT
JWT_SECRET_KEY=tu_clave_jwt_secreta

# Clave secreta para uso interno del Flask app (ej: sesiones)
FLASK_APP_KEY=clave_cualquiera

# Ruta principal de la app Flask
FLASK_APP=src/app.py

# Activar modo debug (solo para desarrollo)
FLASK_DEBUG=1
DEBUG=TRUE

# Stripe (clave secreta para pagos en el backend)
STRIPE_SECRET_KEY=sk_test_tu_clave_secreta_de_stripe


# 👇 Front-End Variables

# Prefijo de rutas si desplegás en subdirectorio (generalmente "/")
BASENAME=/

# URL del backend que consume el frontend (ejemplo: Render)
REACT_APP_BACKEND_URL=https://api-tuapp.onrender.com

# Stripe (clave publicable para cliente)
REACT_APP_STRIPE_PUBLISHABLE_KEY=pk_test_tu_clave_publica_de_stripe
